from setuptools import setup, find_packages

setup(name = "src",
      author="tiagocarvalhocapgemini",
      author_email="tiago.carvalho@capgemini.com",
      version="1.0", 
      packages = find_packages(), 
      setup_requires=['wheel'])